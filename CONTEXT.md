# YTAB

Brave / Chromium 新标签起始页扩展：带秒时钟、Wallhaven 壁纸、一言、Bing 搜索，以及带文件夹与多页的 Launchpad 式 App 网格，替代浏览器默认新标签页。

## Language

**起始页**：
打开新标签时展示的页面，替换浏览器默认新标签内容。
_Avoid_：主页（易与浏览器「打开时的主页」混淆）、仪表盘

**App**：
用户手动添加的网站快捷方式，以应用式磁贴（图标 + 名称）呈现；输入网址后自动抓取图标与名称，均可再改。点击行为可在设置中选「当前标签」或「新标签」，默认当前标签。
_Avoid_：书签（列表行形态）、快捷方式、图标（单指视觉素材时另说）

**首次启动**：
扩展第一次打开起始页时，让用户二选一：**作者默认配置**（预置作者自用的一组 App）或 **零配置**（空 App 网格，自行添加）。选完后不再自动弹出（可在设置中重置引导则另说，非首期必做）。
_Avoid_：引导教程、onboarding 多步向导

**作者默认配置**：
首次启动可选的预置 App 集合（均可删改），当前为：哔哩哔哩、抖音、GitHub、Linux Do、ChatGPT、Claude、Gemini、DeepSeek、Gmail、网易邮箱、Proton Mail。
_Avoid_：系统推荐、热门站点（非个性化话术）

**App 图标**：
某个 App 的视觉标识。来源可以是：打开网址后自动抓取、图片 URL、或本地上传（png / jpg / svg / webp）。自动抓取失败时用占位图，名称回退为主机名，仍可再改。
_Avoid_：favicon（实现细节）、logo

**文件夹**：
App 网格中的分组容器，可放入多个 App；点开后在叠加层中展示其中的 App（对标 macOS Launchpad）。由 **合文件夹** 产生；名称可随后修改（新建时默认叫「文件夹」）。文件夹本身只参与换位，不能再与 App 合成更大容器。空文件夹删除；**只剩 1 个 App 时自动拆开**，该 App 回到网格原文件夹位置。
_Avoid_：分组、分类、集合

**合文件夹**：
把一个 App 拖到另一个 App 上并**短暂停住**后，两者收入同一文件夹的动作。立刻松手则只是换位，不会合文件夹。拖到已有文件夹上时同样：停住才放进，立刻松手只换位。
_Avoid_：合并、叠放、拖放合并（未区分停住与否）

**App 网格**：
起始页上 Launchpad 式的居中图标网格，App 与文件夹在此排列，可拖拽换位与合文件夹；过多时用 **多页**（拖到左右边缘可翻页 + 页码指示点）。按住约一瞬再进入拖拽，以免点按打开被误当成拖。可丢到空白处或按 Esc 取消本次拖拽。文件夹打开后，其中的 App 同样可拖拽换位。
_Avoid_：Dock、桌面图标、磁贴墙、单页滚动（非本产品主交互）

**时钟**：
起始页上的时间展示：时:分:秒，并带日期（星期 / 月日）；默认 24 小时制。
_Avoid_：时间组件、timer

**壁纸**：
起始页全幅背景图；来源为 Wallhaven。仅桌面横屏比例（16:9 / 16:10），文件类型限 jpg/png；显示与缓存按屏幕缩放（最长边不超过 2560）。打开时用已持久化的当前图；内存中另有预取池（约 3 张）供「换一张」秒切。池空时先小图再清晰图，交叉淡入。按本地自然日自动换当前图，日界作废旧池。可配 API Key、纯度与分类。
_Avoid_：背景（过宽）、主题图、原图直链（非显示用尺寸）

**一言**：
起始页上展示的一句短文案；来源为一言（hitokoto.cn）在线 API。每次打开起始页尝试换一句；请求失败时显示上一句的本地缓存。
_Avoid_：语录、每日一句（一言不是「一天只换一次」；壁纸才是日更）

**搜索**：
起始页上的搜索框；首期仅 Bing。默认使用 `cn.bing.com`，可在设置中切换为国际 `www.bing.com`。提交后在当前标签打开结果。
_Avoid_：地址栏、万能框

**设置**：
用户可改的偏好与密钥等；入口为起始页左下角图标。以 **小弹窗** 呈现，视觉与信息架构对标 **macOS 系统设置**（侧栏分类 + 右侧选项）。首期侧栏：**通用**（含开链方式等）/ **壁纸** / **搜索** / **数据**（导入导出、重置所有数据）/ **关于**。界面文案仅中文。整体少铬：轻字影，壁纸与 App 网格为主角。
_Avoid_：选项页、配置中心、抽屉（若与「小弹窗」冲突时以小弹窗为准）、多语言

**导入 / 导出**：
把 App、文件夹、设置等用户数据打包成单个备份文件导出，或从文件恢复；用于备份与换机，而不是浏览器账号云同步。格式为 ZIP 包（扩展名可用 `.ytab`）：内含紧凑元数据与图标等资源文件。默认包含 App 图标，导出时可勾选「不含图标」以缩小体积。Wallhaven API Key 默认不进入导出文件，导出时可勾选「包含密钥」。
_Avoid_：同步、JSON 备份、备份（单独说「导出文件」时仍用导出）
