# YTAB

Brave / Chromium 新标签起始页扩展：带秒时钟、Wallhaven 壁纸、一言、Bing 搜索，以及带文件夹与多页的 Launchpad 式 App 网格，替代浏览器默认新标签页。

## Language

**起始页**：
打开新标签时展示的页面，替换浏览器默认新标签内容。
_Avoid_：主页（易与浏览器「打开时的主页」混淆）、仪表盘

**App**：
用户手动添加的网站快捷方式，以应用式磁贴（图标 + 名称）呈现；输入网址后自动抓取图标与名称，均可再改。右键 App 可**编辑**或**删除**（文件夹里同样）；空白处右键添加。添加与编辑用与设置同类的玻璃小面板。点击行为可在设置中选「当前标签」或「新标签」，默认当前标签。
_Avoid_：书签（列表行形态）、快捷方式、图标（单指视觉素材时另说）

**首次启动**：
扩展第一次打开起始页时，让用户二选一：**作者默认配置**（预置作者自用的一组 App）或 **零配置**（空 App 网格，自行添加）。选完后不再自动弹出（可在设置中重置引导则另说，非首期必做）。
_Avoid_：引导教程、onboarding 多步向导

**作者默认配置**：
首次启动可选的预置 App 集合（均可删改），当前为：哔哩哔哩、抖音、GitHub、Cloudflare、Cursor、Grok、X、Linux Do、ChatGPT、Claude、Gemini、DeepSeek、Gmail、网易邮箱、Proton Mail。
_Avoid_：系统推荐、热门站点（非个性化话术）

**App 图标**：
某个 App 的视觉标识。作者默认配置用扩展内置图；用户添加时自动抓站点提供的最大图标，失败再回退通用小图；也可填图片 URL 或本地上传。自动抓取失败时用占位图，名称回退为主机名，仍可再改。用户上传过的图不自动覆盖。抓取或上传得到的像素缓存在 IndexedDB；chrome.storage 只留 http 地址或引用，以免配额把整包状态打挂。
_Avoid_：favicon（实现细节）、logo、图床

**文件夹**：
App 网格中的分组容器，可放入多个 App；点开后在叠加层中展示其中的 App（对标 macOS Launchpad）。由 **合文件夹** 产生；名称可随后修改（新建时默认叫「文件夹」）。文件夹本身只参与换位，不能再与 App 合成更大容器。空文件夹删除；**只剩 1 个 App 时自动拆开**，该 App 回到网格原文件夹位置。右键文件夹可删除，其中的 App 一并去掉。
_Avoid_：分组、分类、集合

**合文件夹**：
把一个 App 拖到另一个 App 上并**短暂停住**后，两者收入同一文件夹的动作。立刻松手则只是换位，不会合文件夹。拖到已有文件夹上时同样：停住才放进，立刻松手只换位。
_Avoid_：合并、叠放、拖放合并（未区分停住与否）

**App 网格**：
起始页上 Launchpad 式的居中图标网格，App 与文件夹在此排列，可拖拽换位与合文件夹；过多时用 **多页**（拖到左右边缘可翻页 + 页码指示点）。按住约一瞬再进入拖拽，以免点按打开被误当成拖。空格落点：未占用的格、网格下方、网格上方（时钟/搜索一带）、第一颗左侧至翻页热区以外，停住后插入（上/左→首位，右/下→末尾）；角落两向同时成立时按下→左→右→上，因此左下、右上都是末尾。已在目标位置则不动。Esc 取消本次拖拽。文件夹打开后，其中的 App 同样可拖拽换位。右键空白处添加 App；右键 App 编辑或删除。领域上亦指这份可操作状态本身：多页上的 App/文件夹、当前页、打开中的文件夹，以及拖拽会话快照；合文件夹 / 拆文件夹 / 放进文件夹 / 跨页拖 / Esc 回滚等规则的实现落在 `src/lib/appGrid.ts`。
_Avoid_：Dock、桌面图标、磁贴墙、单页滚动（非本产品主交互）、网格文档（勿另造同义名）

**时钟**：
起始页上的时间展示：时:分:秒，粗体（对标 iOS 锁屏那种厚字），并带日期（星期 / 月日）；默认 24 小时制。用系统字体，不另打 webfont。
_Avoid_：时间组件、timer

**壁纸**：
起始页全幅背景图；来源为 Wallhaven。仅桌面横屏比例（16:9 / 16:10），文件类型限 jpg/png；显示与缓存按屏幕缩放（最长边不超过 2560）。打开时用已持久化的当前图；内存中另有预取池（约 3 张）供「换一张」秒切。池空时仍一次成图再交叉淡入。按本地自然日自动换当前图，日界作废旧池。手动「换一张」在起始页右下角，不在设置小弹窗里。失败时用顶部通知说明原因，其中「去设置」为可点链接：打开壁纸分类并点亮对应项；换图按钮旁不再另写原因。Wallhaven 密钥、内容尺度、分类、排序与标签仍在设置中。内容尺度为安全 / 擦边 / 「少儿不宜」；分类为常规 / 动漫 / 人物，默认只开动漫。排序为随机 / 最新 / 相关 / 浏览 / 收藏 / 热门，默认热门（近一个月）。标签跟着当前分类走：常规为风景 / 自然 / 城市等，动漫为少女 / 樱花 / 机甲等，人物为人像 / 街头 / 特写等；多开则并集，关掉某类会去掉只属于它的已选标签。多选当搜索词，全关则不限。未填密钥时不能开「少儿不宜」；清空密钥则把「少儿不宜」关掉。密钥旁「去获取」始终显示，在新标签打开 Wallhaven 账户设置。填了密钥后可测是否可用。测通后标题左边保留成功标记（关掉设置再开仍在），改密钥或测失败时带消失动画去掉；测失败用顶部通知说明无效或网络异常。内容尺度、分类在界面上至少各开一项。
_Avoid_：背景（过宽）、主题图、原图直链（非显示用尺寸）、纯度（界面称内容尺度）

**一言**：
起始页上展示的一句短文案；来源为一言（hitokoto.cn）在线 API。每次打开起始页尝试换一句；点击当前句也可换。换的过程中再点忽略。点换失败仍显示当前句，该句红框横抖，失败文案走顶部通知（连不上 / 被限流 / 没给句子）。打开起始页时后台换句失败仍静默用缓存。请求失败时显示上一句的本地缓存。
_Avoid_：语录、每日一句（一言不是「一天只换一次」；壁纸才是日更）

**搜索**：
起始页上的搜索框；首期仅 Bing。默认国内，可在设置「搜索地区」切到国际。提交后在当前标签打开结果。
_Avoid_：地址栏、万能框

**通知**：
起始页上从左侧滑入的短暂提示（先快后慢），随后从右侧滑出（先慢后快）。同一条文案连点只续时，不重播进出。盖在设置小弹窗和首次启动之上。失败偏红，成功偏绿；左侧为圆环加勾或叉。条子为半圆胶囊（矮条上 squircle 会看起来更方）。换图失败文案里的「去设置 → …」可点，点通知其余地方只溜走。密钥测失败、导入失败、一言失败、换图失败会用；关最后一项内容尺度或分类也会用；导出、导入、重置完成后也会用。测通、换图成功、一言换成功不走通知。
_Avoid_：toast、snackbar、气泡

**设置**：
用户可改的偏好与密钥等；入口为起始页左下角轻图标（无底框方块）。以 **小弹窗** 呈现，信息架构对标 **macOS 系统设置**（侧栏分类 + 右侧选项），材质贴起始页，不要做成另一套不透明系统窗。侧栏：**通用**（打开方式、搜索地区）/ **壁纸** / **数据**（导入导出、重置所有数据）。右侧不重复侧栏分类名，只留关闭。右侧内容超出时用自制细胶囊滑块（藏系统滚动条；滚轮仍走原区域）。每项标题旁用问号收说明，下为控件；通用与数据同一行（标题与问号在左，控件在右）。内容尺度、分类、标签为同色方块（开白底、关淡框）；内容尺度与分类三个选项同一行，标签随分类切换、三列排布；只剩一项内容尺度或分类再点关则顶部通知，不关掉。排序与标题同一行，六个分段直接铺开。打开方式、搜索地区、排序的分段可点选，也可按住左右拖。底部分割线下同一行：GitHub（新标签打开项目仓库）、版本号、官方网站（`ytab.luoye.pro`；未上线时图标禁用）。界面文案仅中文。整体少铬：轻字影，壁纸与 App 网格为主角。
_Avoid_：选项页、配置中心、抽屉（若与「小弹窗」冲突时以小弹窗为准）、多语言

**导入 / 导出**：
把 App、文件夹、设置等用户数据打包成单个文件导出，或从文件恢复；用于换机，而不是浏览器账号云同步。导入会整份替换当前 App、文件夹、设置与壁纸，选文件后须确认；导入、导出、重置完成后用顶部通知。格式为 ZIP 包（扩展名可用 `.ytab`）：内含紧凑元数据与图标等资源文件。导出时弹出选项：默认包含 App 图标（「包含图标」默认开），关掉以缩小体积；不含图标时本地像素改成站点 favicon，禁止把 `data:` / `idb:` 写进备份。Wallhaven 密钥默认不进入导出文件，导出时可打开「包含密钥」。
_Avoid_：同步、JSON 备份、备份（单独说「导出文件」时仍用导出）
