# 备份采用 ZIP 容器（`.ytab`），不用 JSON

用户数据（App、文件夹、设置、可选图标与 API Key）需要导入/导出以便换机。拒绝纯 JSON：图标用 base64 会膨胀且不便。决定导出为 **ZIP 包**（扩展名 `.ytab`）：元数据与图标等资源分文件存放；默认包含图标，可勾选不含图标；API Key 默认不导出、可勾选包含。整包以后若需加密，可对 ZIP 加密而不改逻辑模型。
