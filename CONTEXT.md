# YTAB

Brave / Chromium 新标签起始页扩展：带秒时钟、Wallhaven 壁纸、一言、Bing 搜索，以及带文件夹与多页的 Launchpad 式 App 网格，替代浏览器默认新标签页。

## Language

**起始页**：
打开新标签时展示的页面，替换浏览器默认新标签内容。
_Avoid_：主页（易与浏览器「打开时的主页」混淆）、仪表盘

**App**：
用户手动添加的网站快捷方式，以应用式磁贴（图标 + 名称）呈现；网址输入停住后即自动抓取图标与名称（不必失焦），抓取时图标位播 Face ID 扫描再成功，均可再改。添加时先只有满宽取消，抓成功后保存再出现。右键 App 可**编辑**或**删除**（文件夹里同样）；空白处右键添加。添加与编辑用与设置同类的玻璃小面板。点击行为可在设置中选「当前标签」或「新标签」，默认当前标签。
_Avoid_：书签（列表行形态）、快捷方式、图标（单指视觉素材时另说）

**首次启动**：
扩展第一次打开起始页时，主选择仍是 **作者默认配置** 或 **零配置**。其下一行蓝色小字 **已有账号**：去登录；云端有份并解开后整份落地，不再选那两块。云端还没有则说明一声，回到这两块再选。可以不登录；没登录则卸扩展或换机丢光。选完后不再自动弹出。
_Avoid_：引导教程、onboarding 多步向导、第三块卡片

**作者默认配置**：
首次启动可选的预置 App 集合（均可删改），当前为：哔哩哔哩、抖音、GitHub、Cloudflare、Cursor、Grok、X、Linux Do、ChatGPT、Claude、Gemini、DeepSeek、Gmail、网易邮箱、Proton Mail。
_Avoid_：系统推荐、热门站点（非个性化话术）

**App 图标**：
某个 App 的视觉标识。作者默认配置用扩展内置图；用户添加时自动抓站点提供的最大图标，失败再回退通用小图；也可填图片 URL 或本地上传。自动抓取失败时用占位图，名称回退为主机名，仍可再改。用户上传过的图不自动覆盖。抓取或上传得到的像素缓存在 IndexedDB，不写进设置里的图标输入框（框里只留用户填的 http 地址）；chrome.storage 只留 http 地址或引用，以免配额把整包状态打挂。
_Avoid_：favicon（实现细节）、logo、图床

**文件夹**：
App 网格中的分组容器，可放入多个 App；点开后在叠加层中展示其中的 App（对标 macOS Launchpad）。由 **合文件夹** 产生；名称可随后修改（新建时默认叫「文件夹」）。文件夹本身只参与换位，不能再与 App 合成更大容器。空文件夹删除；**只剩 1 个 App 时自动拆开**，该 App 回到网格原文件夹位置。右键文件夹可删除，其中的 App 一并去掉。
_Avoid_：分组、分类、集合

**合文件夹**：
把一个 App 拖到另一个 App 上并**短暂停住**后，两者收入同一文件夹的动作。立刻松手则只是换位，不会合文件夹。拖到已有文件夹上时同样：停住才放进，立刻松手只换位。
_Avoid_：合并、叠放、拖放合并（未区分停住与否）

**App 网格**：
起始页上 Launchpad 式的居中图标网格，App 与文件夹在此排列，可拖拽换位与合文件夹；过多时用 **多页**（拖到左右边缘可翻页 + 页码指示点）。按住约一瞬再进入拖拽，以免点按打开被误当成拖。空格落点：未占用的格、网格下方、网格上方（时钟/搜索一带）、第一颗左侧至翻页热区以外，停住后插入（上/左→首位，右/下→末尾）；角落两向同时成立时按下→左→右→上，因此左下、右上都是末尾。已在目标位置则不动。Esc 取消本次拖拽。文件夹打开后，其中的 App 同样可拖拽换位。右键空白处添加 App；右键 App 编辑或删除。领域上亦指这份可操作状态本身：多页上的 App/文件夹、当前页、打开中的文件夹，以及拖拽会话快照；合文件夹 / 拆文件夹 / 放进文件夹 / 跨页拖 / Esc 回滚等规则的实现落在 `src/lib/appGrid.ts`。
_Avoid_：Dock、桌面图标、磁贴墙、单页滚动（非本产品主交互）、网格文档（勿另造同义名）

**时钟**：
起始页上的时间展示：时:分:秒，粗体（对标 iOS 锁屏那种厚字），并带日期（星期 / 月日）；默认 24 小时制。用系统字体，不另打 webfont。
_Avoid_：时间组件、timer

**壁纸**：
起始页全幅背景图；来源为 Wallhaven。仅桌面横屏比例（16:9 / 16:10），文件类型限 jpg/png；显示与缓存按屏幕缩放（最长边不超过 2560）。打开时用已持久化的当前图；内存中另有预取池（约 3 张）供「换一张」秒切。池空时仍一次成图再交叉淡入。按本地自然日自动换当前图，日界作废旧池。手动「换一张」在起始页右下角，不在设置小弹窗里。失败时用顶部通知说明原因，其中「去设置」为可点链接：打开壁纸分类并点亮对应项；换图按钮旁不再另写原因。Wallhaven 在设置壁纸页里整块可展开；密钥、尺度、分类、排序与标签收在里面。尺度为安全 / 擦边 / 「少儿不宜」；分类为常规 / 动漫 / 人物，默认只开动漫。排序为随机 / 最新 / 相关 / 浏览 / 收藏 / 热门，默认热门（近一个月）。标签跟着当前分类走：常规为风景 / 自然 / 城市等，动漫为少女 / 樱花 / 机甲等，人物为人像 / 街头 / 特写等；多开则并集，关掉某类会去掉只属于它的已选标签。多选当搜索词，全关则不限。未填密钥时不能开「少儿不宜」；清空密钥则把「少儿不宜」关掉。密钥旁「去获取」始终显示，在新标签打开 Wallhaven 账户设置。填了密钥后可测是否可用。测通后标题左边保留成功标记（关掉设置再开仍在），改密钥或测失败时带消失动画去掉；测失败用顶部通知说明无效或网络异常。尺度、分类在界面上至少各开一项。
_Avoid_：背景（过宽）、主题图、原图直链（非显示用尺寸）、纯度、内容尺度（界面称尺度）

**一言**：
起始页上展示的一句短文案；来源为一言（hitokoto.cn）在线 API。每次打开起始页尝试换一句；点击当前句也可换。换的过程中再点忽略。点换失败仍显示当前句，该句红框横抖，失败文案走顶部通知（连不上 / 被限流 / 没给句子）。打开起始页时后台换句失败仍静默用缓存。请求失败时显示上一句的本地缓存。
_Avoid_：语录、每日一句（一言不是「一天只换一次」；壁纸才是日更）

**搜索**：
起始页上的搜索框；首期仅 Bing。默认国内，可在设置「搜索地区」切到国际。提交后在当前标签打开结果。
_Avoid_：地址栏、万能框

**通知**：
起始页上从左侧滑入的短暂提示（先快后慢），随后从右侧滑出（先慢后快）。同一条文案连点只续时，不重播进出。盖在设置小弹窗和首次启动之上。失败偏红，成功偏绿；左侧为圆环加勾或叉。条子为半圆胶囊（矮条上 squircle 会看起来更方）。换图失败文案里的「去设置 → …」可点，点通知其余地方只溜走。密钥测失败、一言失败、换图失败、登录失败、恢复口令不对、账号备份上传或拉回失败会用；关最后一项尺度或分类也会用；重置完成后也会用。账号动作的成功反馈（已上传 / 已恢复 / 已改口令 / 已登出 / 云端这份已删）也走通知。测通、换图成功、一言换成功不走通知。进出场与续时规则的实现落在 `src/lib/notice.ts`，宿主只渲染。
_Avoid_：toast、snackbar、气泡

**设置**：
用户可改的偏好与密钥等；入口为起始页左下角轻图标（无底框方块）。以 **小弹窗** 呈现，信息架构对标 **macOS 系统设置**（侧栏分类 + 右侧选项），材质贴起始页，不要做成另一套不透明系统窗。侧栏项只有 **通用** / **壁纸**。最上始终是头像条：已登录为头像 + 名字，未登录为默认头像 + 「登录」；点这条打开右侧账号内容（侧栏不再单列「账号」）。上下分割线左右留宽空。无导入导出。账号页两块：云端备份（状态 + 解开或改口令 + 退出）、危险操作（删除云端、重置本机）。退出后本机 App 留下，不再上传。删除云端再确认，只丢掉密文。重置只清本机并忘掉这台登录，云端还在。右侧不重复侧栏分类名，只留关闭。右侧内容超出时用自制细胶囊滑块（藏系统滚动条；滚轮仍走原区域）。每项标题旁用问号收说明，下为控件；通用与账号同一行（标题与问号在左，控件在右）。壁纸页 Wallhaven 整块可展开（换图失败点「去设置」时自动展开）；收起时卡片不留空，展开不裁标题。尺度、分类三个选项与标题同一行并右对齐；标签默认收起，展开后随分类切换、三列排布；只剩一项尺度或分类再点关则顶部通知，不关掉。排序与标题同一行，六个分段直接铺开。打开方式、搜索地区、排序的分段可点选，也可按住左右拖。底部分割线下同一行：GitHub（新标签打开项目仓库）、版本号。界面文案仅中文。整体少铬：轻字影，壁纸与 App 网格为主角。
_Avoid_：选项页、配置中心、抽屉（若与「小弹窗」冲突时以小弹窗为准）、多语言

**账号**：
用 GitHub 登录后的身份；云端那份副本绑在这个身份上。不是浏览器自带账号。Client ID 没配则不显示登录。
_Avoid_：用户（过宽）、浏览器账号、会员

**恢复口令**：
用户自己设的一串，用来加密云端那份。第一次往这家账号上传时设。这台解开后记住，登出或卸扩展就忘。可在账号页用旧口令换成新口令。服务器只见密文。忘了不能找回，只能丢掉云端这份当新用户。不是登录密码。
_Avoid_：密码、PIN、同步口令

**账号备份**：
登录后云端只留一份完整副本（App、文件夹、设置、当前壁纸、图标、Wallhaven 密钥），用恢复口令在本机加密再上传；网格或设置改完自动覆盖，后写盖住先写，不对云端做版本比对。卸扩展或换机后登录并解开，整份拉回。本机已有网格再登录且云端有份：问一次用云端的还是用这台的（选这台会盖住云端）。空白首次且云端有份：直接拉回。不是两台同时改的对齐。没登录不上云。
_Avoid_：云同步、同步、自动备份、导入导出
