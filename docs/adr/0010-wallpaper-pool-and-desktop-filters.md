# 壁纸：桌面过滤 + 显示缩放 + 内存预取池

Wallhaven 拉取增加桌面约束：`ratios=16x9,16x10`，仅 `image/jpeg` / `image/png`，下载后缩放到 `min(屏宽×dpr, 2560)` 再作显示用 data URL，避免 8K 原图拖垮「换一张」。

当前壁纸仍只持久化一张。另维护内存预取池（3 张）：进页先保证当前图，空闲补池；手动换与日更共用取池逻辑，日界清空池。池空时先 `thumbs.large` 再终稿，层间交叉淡入。
