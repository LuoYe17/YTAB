# 壁纸来源为 Wallhaven（可配置 API Key）

起始页壁纸不采用仅本地图库或无 Key 的随意图源，而采用 **Wallhaven**：支持分类/纯度过滤、随机取图与「换一张」。API Key 在设置中配置，用以提高限额与访问能力。默认纯度仅 SFW，三项纯度均可在设置中勾选。

打开起始页时**不每次请求**：使用本地缓存图；**每天自动换一张**；用户也可手动「换一张」（入口在起始页右下角，不在设置小弹窗里）。依赖外网与第三方服务可用性；日界与缓存实现细节以产品后续钉死的规则为准，但不改变「主图源是 Wallhaven + 日更缓存」这一决策。
